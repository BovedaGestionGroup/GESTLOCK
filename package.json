{
  "name": "gestor-contrasenas-empresarial",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "test": "npm run test --workspace apps/api"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
